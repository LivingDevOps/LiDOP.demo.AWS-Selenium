{
  "name": "app",
  "version": "1.0.0",
  "description": "Demo App",
  "main": "app.js",
  "scripts": {
    "start": "node ./src/app.js",
    "test": "mocha ./test/unit",
    "itestp1": "mocha-parallel-tests ./test/integration1 --timeout 120000 --reporter mochawesome --reporter-options reportDir=.results/reports,reportFilename=tests",
    "itestp2": "mocha-parallel-tests ./test/integration2 --timeout 120000 --reporter mochawesome --reporter-options reportDir=.results/reports,reportFilename=tests"
  },
  "author": "Pascal Lauener",
  "license": "ISC",
  "dependencies": {
    "express": "latest",
    "pug": "latest"
  },
  "devDependencies": {
    "chai": "latest",
    "mocha": "latest",
    "mocha-parallel-tests": "latest",
    "mochawesome": "latest",
    "request": "latest",
    "selenium-webdriver": "latest"
  }
}
